/**
 * @module app/components/{{ lowerCase path }}{{ pascalCase name }}.story
 */

import React from 'react';
import { storiesOf } from '@storybook/react';

import {{ pascalCase name }} from './{{ pascalCase name }}';

storiesOf('Components|{{ lowerCase path }}{{ pascalCase name }}', module)
  .add('create story', () => (
    <{{ pascalCase name }} />
  ));
