/**
 * @module app/components/{{ pascalCase name }}.story
 */

import React from 'react';
import { storiesOf } from '@storybook/react';

import {{ pascalCase name }} from './{{ pascalCase name }}';

storiesOf('Components|{{ pascalCase name }}', module)
  .add('create story', () => (
    <{{ pascalCase name }} />
  ));
