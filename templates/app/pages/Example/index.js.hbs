import loadable from '@loadable/component';

export default loadable(() => import(
  /* webpackChunkName: "{{ dashCase name }}" */
  './{{ pascalCase name }}' /* eslint-disable-line comma-dangle */
));
