/**
 * @module app/containers/{{ pascalCase name }}
 */

import PropTypes from 'prop-types';
import React from 'react';
import { connect } from 'react-redux';

import ExampleComponent from 'app/components/...';
import { getSelector } from 'app/state/...';

const {{ pascalCase name }} = props => {
  return (
    <ExampleComponent />
  );
};

{{ pascalCase name }}.propTypes = {
};

const mapStateToProps = state => ({
  text: getSelector(state),
});

export default connect(mapStateToProps)({{ pascalCase name }});
